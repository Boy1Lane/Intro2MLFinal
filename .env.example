# Backend config — copy to .env (auto-loaded by app/backend/config.py)
ARTIFACTS_DIR=artifacts
PHOBERT_REPO=phobert          # local dir or HF Hub repo id of fine-tuned PhoBERT
CORS_ORIGINS=http://localhost:3000
GEMINI_API_KEY=               # Google AI Studio key; enables /rewrite
HF_TOKEN=